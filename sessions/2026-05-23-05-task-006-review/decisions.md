# Решения — task-006-review

| # | Решение | Альтернативы | Обоснование | Где зафиксировано |
|---|---|---|---|---|
| 1 | `tests/conftest.py` перенесён в `tests/unit/conftest.py`; integration получает env из `.env`/CI workflow | Общий conftest с pytest-маркер-фильтром; conditional setdefault по маркеру | Природа тестов разная — у unit нет реальной БД, у integration есть. Чёткое разделение по директориям естественнее условной логики в одном файле | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`tests/unit/conftest.py`](../../tests/unit/conftest.py) |
| 2 | Integration-тесты дёргают `alembic` через `subprocess`, не in-process | `alembic.command.upgrade(Config(...), ...)` напрямую | Тестируем CLI-поверхность, которая совпадает с `make migrate`; in-process переиспользовал бы event loop тестов; 3.5с на 4 теста — норма для миграций (которые меняются редко) | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`tests/integration/test_migrations.py`](../../tests/integration/test_migrations.py) |
| 3 | `engine` — module-level constant, не lazy `get_engine()` | Lazy fabric с `@lru_cache` | Все пути импорта защищены conftest'ом (unit) или прямым env (integration/prod). Lazy добавил бы две лестницы (config + engine) | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`src/shared/db.py`](../../src/shared/db.py) |
| 4 | Мини-парсер `.env` в `tests/integration/test_migrations.py` | Добавить `python-dotenv` в dev-deps | 10 строк против отдельной зависимости; используется только здесь. Если понадобится ещё где-то — добавим явно | [`state/DECISIONS.md`](../../state/DECISIONS.md) |
| 5 | В CI job `integration` нет explicit teardown postgres-service | `if: always()` cleanup-шаг | Service-container умирает вместе с runner'ом, GitHub Actions сам всё снесёт. Внешних managed-ресурсов нет | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) |
| 6 | Бывший пункт BACKLOG «репозитории/сервисы заготовки» разделён на TASK-007 (только repos) и TASK-008 (только services); все последующие задачи сдвинуты на +1 | Оставить как было (одна задача на repos+services) | Каждая задача получает чёткий фокус и тестируемую границу. Repos — query-слой без транзакций; services — композиция + транзакции + доменные исключения. Размер PR-ов остаётся обозримым | [`state/BACKLOG.md`](../../state/BACKLOG.md), TASK-007 |
