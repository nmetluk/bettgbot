# Решения — task-016-review

**Все 3 — keep:**

| # | Решение | Альтернативы | Обоснование |
|---|---|---|---|
| 1 | `# noqa: ARG001` на unused params не нужны — оставляем без suppressions | Префиксы `_user`/`_session`; включить ARG001 в ruff | Правило ARG001 в `pyproject.toml` не enabled — suppressions избыточны. Включать ARG001 без необходимости — добавлять шум во все handler'ы, которые принимают DI-параметры по контракту. Если когда-нибудь включим — точечно поменяем на префикс или явно подавим |
| 2 | Сигнатура `cmd_help(message, user, session)` — все три параметра остаются | Убрать `session` (не используется в этом handler'е) | **Стилевая норма проекта**: все 6 entry-point handler'ов (cmd_start, cmd_events, cmd_predict, cmd_my, cmd_reminders, cmd_help) принимают одинаковый набор. Это упрощает чтение, тестирование (одинаковые моки), и онбординг новых разработчиков. Минимизировать ради 2 строк — преждевременная оптимизация |
| 3 | Integration-тест на `exc.reason` — opt-in, не доделываем сейчас | Доделать в этом PR (закрепить контракт) | DoD помечал это как «опционально». В этой задаче integration-тесты не трогались. **Триггер для добавления**: следующая задача, которая правит `ReminderService._validate_offsets` или его integration-тесты — там вписать `assert exc.reason == "..."` для каждой ветки |
