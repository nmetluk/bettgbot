---
id: TASK-073
created: 2026-05-30
author: cowork-agent
parallel-safe: true
blockedBy: []
related:
  - handoff/archive/TASK-071.md
  - docs/audit/2026-05-30-design-audit.md
  - src/admin/static/js/analytics.js
priority: low
estimate: S
---

# TASK-073: график аналитики — размеры canvas и пустое состояние (D5)

## Контекст

D5 из дизайн-аудита (`docs/audit/2026-05-30-design-audit.md`). В TASK-071 исполнитель
осознанно отложил этот пункт, но отдельный тикет не завёл — заводим здесь.
Низкий приоритет, к доступности не относится, чисто визуальная доводка.

## Цель

Привести график на `/analytics` к аккуратному виду: canvas на всю ширину карточки,
заданная высота, корректное пустое состояние при 0 прогнозов.

## Definition of Done

> 🚨 Перед `chore(handoff): archive` — ОБЯЗАТЕЛЬНО `handoff/outbox/TASK-073-report.md`.
> 🚨 Задача не закрыта, пока CI зелёный и PR смёржен.

- [ ] `<canvas>` графика растянут на ширину карточки-контейнера, высота задана явно
      (через CSS-токен/класс, не инлайн-хардкодом, если есть подходящее место).
- [ ] Пустое состояние (0 прогнозов / нет данных за период) показывает понятный плейсхолдер,
      а не пустой/схлопнутый прямоугольник. Текст плейсхолдера — через i18n (`src/bot/texts.py`
      или соответствующий admin-словарь, если он отдельный), не хардкодом в шаблоне.
- [ ] Проверено визуально: с данными и без; график не выходит за карточку, не «прыгает»
      при ресайзе.
- [ ] CSP-совместимость не нарушена (никакого инлайн-скрипта; см. TASK-060 — графики уже
      через внешний JS).
- [ ] `ruff`/`mypy src/shared`/`pytest` зелёные; PR `TASK-073: analytics canvas sizing + empty state`;
      CI зелёный; PR смёржен; локальная `main` синхронизирована.
- [ ] Отчёт + archive; inbox чист.

## Вне скоупа

- Новые типы графиков, смена библиотеки, доп. метрики — только размеры/пустое состояние.

## Артефакты

- `* src/admin/static/js/analytics.js` — размеры canvas, рендер пустого состояния
- `* шаблон страницы аналитики` — контейнер/высота, плейсхолдер
- `* handoff/outbox/TASK-073-report.md`

## Ссылки

- Источник: [`docs/audit/2026-05-30-design-audit.md`](../../docs/audit/2026-05-30-design-audit.md) (D5)
- CSP-контекст графиков: TASK-060 (графики через внешний JS)
