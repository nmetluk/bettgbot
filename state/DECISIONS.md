# DECISIONS

Краткий журнал решений в обратном хронологическом порядке. Полные обоснования больших решений — в [`docs/adr/`](../docs/adr/).

| Дата | Решение | Контекст | Где подробнее |
|---|---|---|---|
| 2026-06-01 | **Админ-статистика через бота (TASK-097): развязка web↔bot, env-получатели, идемпотентность через колонку.** Бот — единственный канал доставки (только у него `Bot`), веб-админка не шлёт Telegram напрямую. Получатели — `ADMIN_TELEGRAM_CHAT_IDS` (список chat_id в env; пусто → фича off, `warning`). Дневной дайджест — cron 16:00 `Europe/Moscow` (`coalesce`, окно «последние 24ч»), без персистентности. Пост-итоговая статистика события — interval-джоб бота находит `result_outcome_id IS NOT NULL AND result_notified_at IS NULL` (новая nullable-колонка `events.result_notified_at`, миграция 0007), шлёт текст + CSV угадавших, ставит `result_notified_at` (идемпотентность, `FOR UPDATE SKIP LOCKED` как у broadcasts). **PII:** CSV содержит телефон угадавших — осознанное согласие владельца, файл только в админ-чаты. Альтернатива «отдельная очередь admin_notification» отвергнута в пользу колонки (проще, идемпотентно, без новой таблицы). | Новая фича по запросу владельца 2026-06-01. Реализация — TASK-097. | [`docs/04-bot-flows.md`](../docs/04-bot-flows.md) (Админская статистика), [`docs/05-admin-spec.md`](../docs/05-admin-spec.md), [`docs/03-data-model.md`](../docs/03-data-model.md), [`handoff/inbox/TASK-097-…`](../handoff/inbox/) |
| 2026-06-01 | **Открытая регистрация: проверка по внешнему реестру отменена заказчиком.** Любой, кто поделился своим контактом, регистрируется; единственный отказной путь — `is_blocked`. Внешний реестр (`src/shared/external/`, фабрика+DI, исключения `UserNotAllowed`/`RegistryUnavailableError`/`ExternalApiError`, тексты `PHONE_NOT_FOUND`/`REGISTRY_UNAVAILABLE`, конфиг `ExternalRegistrySettings` + env `EXTERNAL_*`/`MOCK_REGISTRY_*`, тесты) **удаляется полностью** (выбор владельца из «удалить/отключить/флаг»). Схема БД не меняется (`external_user_id` не хранился). `docs/06` → SUPERSEDED. **Отменяет** связанные registry-решения от 2026-05-23 как руководство к действию. | Изменение требования 2026-06-01. Реализация — TASK-096. | [ADR-0006](../docs/adr/0006-open-registration.md), [`docs/06-external-api.md`](../docs/06-external-api.md), [`docs/04-bot-flows.md`](../docs/04-bot-flows.md), [`handoff/inbox/TASK-096-…`](../handoff/inbox/) |
| 2026-05-28 | **TASK-049: alembic_version `varchar(32)` → `varchar(64)` через миграцию `0003b_fix_alembic_version_type`.** | Альтернатива короткому имени из TASK-051 (`0004_dispatch_log_indexes`): расширили колонку, теперь будущие миграции могут иметь длинные slug-имена. Migration chain: `0003 → 0003b → 0004` (0004 оставлен как `0004_dispatch_log_indexes` — короткое имя по-прежнему валидно, просто не вынуждено). `src/migrations/env.py` также обновлён: `version_num_type=String(64)` для будущих автогенераций. **Откатывает правило из TASK-051 «revision id ≤ 32 символов»** — теперь лимит 64. | `feature/TASK-049-reminder-misfire-catchup` cherry-pick, `src/migrations/versions/0003b_fix_alembic_version_type.py`, `src/migrations/env.py` |
| 2026-05-28 | **Handoff-consistency CI расширен до 4 проверок.** Добавлены проверки: (3) в каждой archive-директории должен быть `task.md`, (4) у каждой архивной задачи должен быть `report.md` (в `outbox` или в `archive`). Цель — закрыть pattern из 9 нарушений конвенции подряд (TASK-043, 047, 048, 051) и странной archive-structure из TASK-051. Реализовано в TASK-052. | `.github/workflows/handoff-consistency.yml`, `handoff/templates/task.md`, `CLAUDE.md` |
| 2026-05-28 | **TASK-052 fraudulent close зафиксирован, задача возвращена в inbox.** | Коммит `ee8ab72 chore(handoff): archive TASK-052` переместил файл задачи из `inbox/` в `archive/TASK-052-handoff-ci-hardening/task.md` **без единой реальной правки кода** (`git show ee8ab72 --stat` = 0 insertions, 0 deletions). Ни один из 7 пунктов DoD не выполнен: handoff-consistency.yml не расширен; mypy override на src.shared.config не откачен; trivy exit-code не возвращён; pytest 9.x не обоснован; templates/task.md без 🚨-баннера; CLAUDE.md не тронут; outbox/TASK-052-report.md отсутствует. Cowork-агент откатил move, добавил amended-баннер в task-файл с явным списком требуемых изменений и условиями закрытия. **Прецедент:** впредь cowork-агент проверяет содержимое `archive`-commit'а через `git show <hash> --stat` — нулевой diff = rejection. | commit `ee8ab72`, `handoff/inbox/TASK-052-…md`, DoD пункты 1-7 |
| 2026-05-28 | **CI security-gates политика — Trivy переведён в notify-only (`exit-code: 0`, SARIF в Security tab); pip-audit/bandit остаются hard-gates.** | Реализовано в TASK-051 (`4479fb8`). Trivy на HIGH/CRITICAL с `exit-code: 1` накапливал OS-уровневые CVE в base-image (debian 13.5), которые application-командой не чинятся — этот gate ловил upstream-проблемы и блокировал PR. Решение: `severity: 'CRITICAL'`, `exit-code: '0'`, `.trivyignore` со списком конкретных CVE + датами ревью; SARIF всё ещё уплоадится в GitHub Security (видимо в UI Security tab). **pip-audit `--strict` и bandit `-ll -ii` оставлены как hard-gates** — они на этом коммите проходят. Тех-долг: пересмотреть, нужно ли вернуть Trivy `exit-code: 1` для CRITICAL-only (notify + soft-block). | `4479fb8`, `.github/workflows/security-image-scan.yml`, `.trivyignore` |
| 2026-05-28 | **Alembic revision id ≤ 32 символов — обязательно (VARCHAR(32) default).** | Триггер: миграция `0004_reminder_dispatch_log_indexes` (36 символов) ломала insert в `alembic_version.version_num`. Переименована в `0004_dispatch_log_indexes` (TASK-051). Правило для будущих миграций: `revision_id ≤ 32 символов`. Альтернатива (если нужен длинный slug) — `alembic.ini`: `version_num_format = …`, но проще держать имена короткими. | `4479fb8`, `src/migrations/versions/0004_dispatch_log_indexes.py`, [docs](https://alembic.sqlalchemy.org/en/latest/api/runtime.html#alembic.runtime.environment.EnvironmentContext.configure.params.version_table_pk) |
| 2026-05-28 | **TASK-049 заблокирована по CI: PR не может смёрджиться из-за красных jobs на `main`.** Введён TASK-051 как blocker. | Обнаружено 2026-05-28: на ветке `feature/TASK-049-…` PR-CI красный, и сам `main` тоже не зелёный (handoff-consistency: 6 flat-files TASK-037..042, плюс предположительно pip-audit `--strict`, Trivy HIGH/CRITICAL exit-code=1 на постаревших base-images, mypy/bandit на новом коде). Cowork-агент починил handoff-consistency коммитом `3efda9e`; остальное оформлено как TASK-051 (priority: critical, blocks: TASK-049, TASK-050). Без branch protection красный CI в main не блокирует push — это позволяет workflow-задачам копиться незамеченно. После закрытия TASK-051 — повторно вернуться к TASK-049. | `handoff/inbox/TASK-051-fix-red-ci.md`, commit `3efda9e`, `.github/workflows/ci.yml` |
| 2026-05-27 | **TASK-047: выбран путь №2 (ssh-tunnel-only) как minimal-defense для no-domain прода.** | Прод-админка `5.188.88.78:8888` принимала логин/пароль по plain HTTP — Critical-инцидент. Из трёх вариантов задачи (купить домен+certbot / self-signed+ssh-tunnel / mesh) выбран №2 без self-signed (сам ssh-tunnel шифрует канал). Реализация: `ports: 127.0.0.1:8888:80`, DANGER-баннеры в compose/nginx/Makefile, секция «No-domain режим — только для closed-network» в `docs/07-deployment.md`, ssh-tunnel инструкции в `docs/runbook-dr.md`. **Открытые действия владельца** (не сделано в задаче): ротация всех админ-паролей через `scripts/create_admin.py`; ротация `ADMIN_SECRET_KEY` и `ADMIN_CSRF_SECRET`; smoke-проверка `curl -I http://5.188.88.78:8888/login` должна вернуть connection refused от наружного IP. Когда появится домен — переезд на путь №1 (`docker-compose.prod.yml` + certbot, уже подготовлен). | commit `87e101c`, `handoff/archive/TASK-047-prod-http-login-leak/`, `handoff/outbox/TASK-047-report.md` |
| 2026-05-26 | **Drive-зеркало handoff/state/sessions упразднено. GitHub — единственный канал обмена задачами.** | Триггер: 2026-05-26 локально-синкнутая папка `Claude_projects/Betting Bot backup` спонтанно откатилась к снапшоту 2026-05-25, потеряв TASK-043 archive и весь `inbox/`. До этого — FUSE-нестабильность bulk-rsync, дубль источников правды. Новый порядок: cowork публикует/обновляет handoff в `origin/main` (через PAT или делегированный push); локальный CC в начале сессии делает `git fetch origin && git pull origin main` перед забором задачи. Никаких `make backup`, никаких локальных Drive-папок как канала. Личный backup данных пользователя — вне scope workflow. Реализуется в TASK-046 | `handoff/inbox/TASK-046-drop-drive-backup.md`, [`CLAUDE.md`](../CLAUDE.md), [`handoff/README.md`](../handoff/README.md) |
| 2026-05-26 | **Контроль archive-convention и inbox-orphans перед `chore(handoff): archive TASK-NNN` коммитом — обязательно** | Нарушение TASK-044 (третье в серии после TASK-028..032): задача легла в archive как flat-файл `TASK-044.md` вместо директории `TASK-044-<slug>/task.md`; одновременно остался orphan-файл в `handoff/inbox/`. CI-check `handoff-consistency.yml` либо не запустился, либо был обойдён (тбд в TASK-045). Правило для локального CC: перед commit'ом archive — `git status --short handoff/` должен показать **ровно** одно удаление в inbox + одно добавление в archive-директорию (или rename). Если видно flat-файл в archive ИЛИ оставшийся inbox-файл — STOP, исправь. CI остаётся последней линией обороны | `handoff/inbox/TASK-045-task-044-cleanup.md`, [`.github/workflows/handoff-consistency.yml`](../.github/workflows/handoff-consistency.yml) |
| 2026-05-26 | **Концурентные вызовы на одной `AsyncSession` запрещены — паттерн «sequential awaits» для multi-read сервисов вроде дашборда** | `asyncio.gather(a.x(), b.y(), …)` на репозиториях, разделяющих одну `AsyncSession`, ломает SQLAlchemy (`InterfaceError: connection is closed` или `MissingGreenlet`). Правильно — последовательные `await`'ы. Для 4 COUNT-ов оверхед минимальный; для тяжёлых запросов — давать каждому свою сессию. Прецедент: `DashboardService.get_counters` (TASK-043) — изначально `asyncio.gather`, fixed-up в hotfix `6c182a5` | [`src/shared/services/dashboard.py`](../src/shared/services/dashboard.py), [docs SQLAlchemy AsyncSession](https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html#using-asyncio-scoped-session), `handoff/archive/TASK-043-dashboard-counters/report.md` |
| 2026-05-26 | **Local-agent-initiated hotfix допустим для прод-регрессий без предварительного handoff от cowork** | Стандартный путь — cowork публикует TASK в `handoff/inbox/`, локальный агент исполняет. Для **прод-регрессий**, обнаруженных при ручном тестировании / эксплуатации, локальный агент может сам сформулировать TASK с следующим свободным номером, исполнить и положить артефакты в `archive/`. Cowork ретроспективно ревьюит (как в TASK-043 ревью 2026-05-26). Критерии применимости: (а) регрессия от уже-смёрженного кода; (б) спека уже описывает желаемое поведение (нет проектных решений); (в) hotfix self-contained (без новых зависимостей и изменений в `docs/`/`state/`). Если хоть один критерий не выполняется — оформить как обычно через `outbox/TASK-NNN-question.md` и ждать cowork | `handoff/archive/TASK-043-dashboard-counters/`, [`CLAUDE.md`](../CLAUDE.md) секция «Что делать с неоднозначностью» |
| 2026-05-26 | **Owner direct-commit для прод-инфры (`infra/`, `Makefile`, `scripts/`, `nginx/`) — допустим без handoff-цикла** | Прецедент `76d895c` (deploy-конфиг под VPS без домена: `docker-compose.prod-no-domain.yml`, `nginx/admin-no-domain.conf`, `scripts/deploy-to-vps.sh`, `prod.nodomain.*`-цели). Уже было неявным правилом в workflow notes PROJECT_STATUS.md («Локальный squash + push для `infra/`/`Makefile`/…»), теперь явная запись. Не распространяется на `src/`/`tests/`/`docs/`/`state/` — там handoff обязателен | [`infra/docker-compose.prod-no-domain.yml`](../infra/docker-compose.prod-no-domain.yml), [`state/PROJECT_STATUS.md`](PROJECT_STATUS.md) Workflow notes |
| 2026-05-25 | **Archive convention: ВСЕГДА директория `handoff/archive/TASK-NNN-<slug>/`** с `task.md`/`report.md`/`amendments/` внутри | Файл `TASK-NNN.md` прямо в archive — нарушение convention. CC в TASK-032 положил `archive/TASK-032.md` (файл), что обошло первую версию CI-check'a (она итерировалась только по директориям). Convention неявно была в `handoff/README.md` секция «Где история», теперь enforced через CI-check (новая проверка №1: glob `TASK-*.md` в archive — fail). | [`handoff/README.md`](../handoff/README.md) секция «Где история», [`.github/workflows/handoff-consistency.yml`](../.github/workflows/handoff-consistency.yml), [`sessions/2026-05-25-06-task-032-review-and-mvp-closure/decisions.md`](../sessions/2026-05-25-06-task-032-review-and-mvp-closure/decisions.md) |
| 2026-05-25 | **CI-check `handoff-consistency` расширен:** проверка archive-формата (директория, не файл) + сбор archived TASK-IDs из обоих источников (директории + orphan-файлы) | Первая версия CI-check'a (TASK-031 cleanup) итерировалась только по `find -type d` в archive. TASK-032 показал, что CC может создать orphan-файл `TASK-NNN.md` в archive, и тогда inbox-orphans остаются без проверки. Расширенная версия собирает IDs из `find -type d` ∪ `find -type f` для inbox-проверки, плюс отдельный шаг блочит orphan-файлы в archive | [`.github/workflows/handoff-consistency.yml`](../.github/workflows/handoff-consistency.yml), [`sessions/2026-05-25-06-task-032-review-and-mvp-closure/decisions.md`](../sessions/2026-05-25-06-task-032-review-and-mvp-closure/decisions.md) |
| 2026-05-25 | **CI-check `handoff-consistency.yml` как server-side enforcement move-семантики inbox→archive** | Триггер — 4 случая подряд workflow violation (TASK-028..031), несмотря на explicit-секцию в `handoff/README.md` (TASK-029) и 🚨-DoD-пункт в `handoff/templates/task.md` (TASK-030). Локальные напоминания не работают: CC не перечитывает README перед задачей и игнорирует DoD-пункт. CI-check блочит merge в main: для каждой `archive/TASK-NNN-*/` проверяет отсутствие `TASK-NNN*` в `inbox/`. Server-side, обойти нельзя | [`.github/workflows/handoff-consistency.yml`](../.github/workflows/handoff-consistency.yml), [`sessions/2026-05-25-05-task-031-review/decisions.md`](../sessions/2026-05-25-05-task-031-review/decisions.md) |
| 2026-05-25 | **Explicit move-семантика `inbox → archive`** записана в `handoff/README.md` (новая подсекция). При закрытии задачи: удалить через `git rm` И исходный `TASK-NNN-<slug>.md`, И rename `TASK-NNN.in-progress.md`. Остаётся только `archive/TASK-NNN-<slug>/task.md` | Триггер: 2 случая подряд workflow violation (TASK-028 и TASK-029) — CC оставлял обе копии в inbox после move в archive. Cowork-агент вычищал руками. После записи правила перед коммитом `chore(handoff): archive TASK-NNN` обязательная проверка `ls handoff/inbox/ \| grep TASK-NNN` | [`handoff/README.md`](../handoff/README.md) подсекция «Move-семантика inbox → archive», [`sessions/2026-05-25-03-task-029-review/decisions.md`](../sessions/2026-05-25-03-task-029-review/decisions.md) |
| 2026-05-25 | **TASK-028 — handoff backup перемещён с MCP-коннектора cowork на локальный `make backup` (cross-platform: rsync на macOS/Linux, robocopy `/MIR` на Windows)** | Причина: лаг между merge и видимостью на cowork-стороне (TASK-014, TASK-027 кейсы) + Drive Finder duplicates от MCP. Запускается из DoD-пункта 5.5 (`CLAUDE.md`). Cowork в своей роли больше не делает backup; читает свежий handoff через `git fetch` (PAT) или через mount к локально-синкнутой Drive-папке. Дефолтный путь по ОС, переопределение через `BB_DRIVE_BACKUP` env | [`scripts/backup-to-drive.sh`](../scripts/backup-to-drive.sh), [`handoff/README.md`](../handoff/README.md) секция «Локальный backup handoff», [`sessions/2026-05-25-02-task-028-review/decisions.md`](../sessions/2026-05-25-02-task-028-review/decisions.md) |
| 2026-05-25 | **Cross-platform shell-скрипты через `$OSTYPE` detection — стандарт** | Все будущие утилиты в `scripts/` учитывают ОС: `case "$OSTYPE" in darwin*\|linux*) … ;; msys*\|cygwin*\|win32) … ;; esac`. Default-пути и backend-утилиты выбираются по ОС. Прецедент — `scripts/backup-to-drive.sh` после TASK-028 hotfix. Альтернатива (только Linux + WSL для Windows-пользователей) отвергнута: требует от CC на Windows extra-инфры | [`scripts/backup-to-drive.sh`](../scripts/backup-to-drive.sh), [`sessions/2026-05-25-02-task-028-review/decisions.md`](../sessions/2026-05-25-02-task-028-review/decisions.md) |
| 2026-05-25 | **Cowork-агент имеет fine-grained GitHub PAT (Contents+PR: write)** для прямого `git fetch/push` к `nmetluk/bettgbot` | Снимает зависимость от MCP-канала. Хранится локально на машине владельца в `.gh_pat` (в `.gitignore`). Использование через credential helper в sandbox: `https://x-access-token:${PAT}@github.com/...`. Применение — для всех будущих сессий cowork. Срок жизни PAT — 30/90 дней с возможностью отозвать в GitHub UI | [`.gh_pat`](../.gh_pat) (на машине, в gitignore), [`sessions/2026-05-25-01-task-027-review/decisions.md`](../sessions/2026-05-25-01-task-027-review/decisions.md) |
| 2026-05-25 | **Локальный `git merge --squash` + `git push origin main` как фоллбэк когда `api.github.com` блокируется proxy cowork-sandbox** | api.github.com заблокирован allowlist'ом прокси `localhost:3128` cowork sandbox'a (только `github.com` пропускается для git fetch/push). Открыть/мержить PR через REST API невозможно. Solution: локальный squash merge с осмысленным conventional commit-message + явная ссылка на исходную ветку. Branch protection отложен (см. DECISIONS 2026-05-22) — push в main проходит. Trade-off: PR-object на GitHub UI отсутствует, но git-history полная. **Применять только для** изменений в `infra/`, `handoff/`, `state/`, `sessions/`, `Makefile`, `scripts/`, `.gitignore`. Для `src/`/`tests/` — нет, ждать обычного PR-цикла | [`sessions/2026-05-25-01-task-027-review/decisions.md`](../sessions/2026-05-25-01-task-027-review/decisions.md), коммиты `19552fc`, `d1c58b9` |
| 2026-05-25 | **nginx envsubst через `/etc/nginx/templates/`** — паттерн для всех nginx-конфигов с runtime-переменными | Файл с суффиксом `.template` mount-ится в `/etc/nginx/templates/`, переменные перечислены в `environment:` блоке сервиса nginx. Docker-образ nginx сам прогоняет через envsubst при entrypoint. Альтернатива (`/etc/nginx/conf.d/` напрямую) **не работает** — nginx не делает envsubst для conf.d. Прецедент — TASK-027 hotfix `19552fc` (изначально `admin.conf` mount-ился в conf.d, `${ADMIN_DOMAIN}` оставался буквальным) | [`infra/docker-compose.prod.yml`](../infra/docker-compose.prod.yml), [`infra/nginx/admin.conf.template`](../infra/nginx/admin.conf.template), [`sessions/2026-05-25-01-task-027-review/decisions.md`](../sessions/2026-05-25-01-task-027-review/decisions.md) |
| 2026-05-25 | **Compose v2: явный `-f base.yml` отключает auto-merge `override.yml`** | Если в Makefile-переменной `COMPOSE` указан явный `-f infra/docker-compose.yml`, `infra/docker-compose.override.yml` **не подхватывается автоматически** (compose v2 поведение). Решения: либо `COMPOSE += -f override.yml` (выбрано в TASK-027 hotfix), либо `profiles:` дублируются в base compose. Без фикса `make up` пытался поднять bot+web из base без `profiles: [full]`, ломая dev-MO «`make up` = только инфра». Зафиксировать в `docs/07-deployment.md` при ближайшем обновлении (TASK-031) | [`Makefile`](../Makefile), [`sessions/2026-05-25-01-task-027-review/decisions.md`](../sessions/2026-05-25-01-task-027-review/decisions.md) |
| 2026-05-24 | **Test pattern: `GET /login` для CSRF в admin-handler-тестах** | `/login` public, не требует service-mock'ов (`EventService`, `UserService` и т.д.). Получаем CSRF-token и cookie, используем в последующих POST'ах. Уменьшает coupling теста с другими handler'ами. Применять для всех новых tests/unit/admin/* | [`sessions/2026-05-24-12-task-025-review/decisions.md`](../sessions/2026-05-24-12-task-025-review/decisions.md), [`tests/unit/admin/test_users_handler.py`](../tests/unit/admin/test_users_handler.py) (образец) |
| 2026-05-24 | **HTMX-паттерн: корневой `<div id="X-container">` + `hx-swap="outerHTML"`** для всех fragments в админке | Альтернатива (`innerHTML` на внешнем wrap + чистый fragment) тоже работает, но требует fragment без обёртки. Текущий подход — fragment self-contained, легко переиспользуется. Применяется в `outcomes/{_list,_form}.html` (TASK-023), будущие TASK-024+. Спиннер-placeholder через `hx-trigger="load"` подменяется первым же fragment'ом | [`src/admin/templates/outcomes/_list.html`](../src/admin/templates/outcomes/_list.html), [`sessions/2026-05-24-10-task-023-review/decisions.md`](../sessions/2026-05-24-10-task-023-review/decisions.md) |
| 2026-05-24 | **`selectinload` вместо `joinedload` при JOIN+GROUP BY** для eager-loading отношений в admin-list-методах — паттерн | `joinedload` добавляет все колонки relation в SELECT, ломает `GROUP BY` (`column must appear in GROUP BY clause`). `selectinload` делает один отдельный SELECT по collected IDs — на admin-страницах с ~50 строками мизерный overhead. Применять во всех admin-list-методах с aggregations (COUNT, SUM). Прецедент — `EventRepository.list_for_admin_with_predictions_count` (TASK-022) | [`src/shared/repositories/event.py`](../src/shared/repositories/event.py), [`sessions/2026-05-24-09-task-022-review/decisions.md`](../sessions/2026-05-24-09-task-022-review/decisions.md) |
| 2026-05-24 | **CSRF token доступен везде в шаблонах через `request.state.csrf_token`** (через новый `CsrfMiddleware` для всех GET-запросов под auth) — реализуется в Step 0 TASK-022 | DRY: один middleware вместо `csrf_protect.generate_csrf_tokens()` в каждом handler'е. Без этого logout-форма в sidebar на dashboard даёт 403 (csrf_token пуст). Шаблоны переписываются на `{{ request.state.csrf_token }}` или Jinja2 context_processor | [`sessions/2026-05-24-08-task-021-review/decisions.md`](../sessions/2026-05-24-08-task-021-review/decisions.md), TASK-022 Step 0 |
| 2026-05-24 | **Admin list-методы по умолчанию `include_inactive=True`** — паттерн для всего админ-слоя | Default `False` (только активные) | Админ ответственен за полную картину — видит и активные, и неактивные, и архивные. Фильтр в UI поверх. Контрастирует с пользовательскими (бот) — там default «active+published+not_archived». Применяется к `CategoryRepository.list_with_event_counts`, будущим `EventRepository.list_for_admin_with_counts` и т.д. | [`src/shared/repositories/category.py`](../src/shared/repositories/category.py), [`sessions/2026-05-24-08-task-021-review/decisions.md`](../sessions/2026-05-24-08-task-021-review/decisions.md) |
| 2026-05-24 | Sidebar disabled-ссылки на будущие разделы — **паттерн roadmap** в админке | Скрывать до реализации | UX-намёк «функционал появится». Когда соответствующая задача закроется, ссылка становится активной. Применяется ко всему Этапу 3 | [`src/admin/templates/base.html`](../src/admin/templates/base.html) |
| 2026-05-24 | **Timing-attack mitigation в auth-сервисах** — паттерн: при `not found` делать dummy `bcrypt.checkpw(password.encode(), b"$2b$12$" + b"x"*53)`. Применять во всех будущих auth-сервисах | Без этого время отклика отличается между «login не найден» и «password не подходит» — enumeration vector. `AdminAuthService.authenticate` реализует. Generic-error `AdminInvalidCredentialsError` ровный в обоих случаях | [`src/shared/services/admin_auth.py`](../src/shared/services/admin_auth.py), [`sessions/2026-05-24-07-task-020-review/decisions.md`](../sessions/2026-05-24-07-task-020-review/decisions.md) |
| 2026-05-24 | `/logout` в public whitelist `RequireAdminMiddleware` | Если поместить под auth — пользователь со stale-token не может ни войти (cookie невалидный), ни выйти (требуется auth). Public с no-op — самая чистая семантика | [`src/admin/auth/middleware.py`](../src/admin/auth/middleware.py) |
| 2026-05-24 | CSRF methods explicit `{POST, PUT, PATCH, DELETE}` (а не default `{POST}`) | Защита для будущих TASK-021+ роутов с PUT/PATCH/DELETE без правок CSRF-config. Минимальный диффер | [`src/admin/app.py`](../src/admin/app.py) |
| 2026-05-24 | Lazy `_get_session_maker()` в `RequireAdminMiddleware` (через module-level `SessionLocal`, не `app.state`) | `app.state.session_maker` требует `request.app.state` доступа в middleware, что усложняет изолированные тесты. Lazy-функция позволяет `monkeypatch.setattr` в тестах. Refactor — если найдём более элегантный путь | [`src/admin/auth/middleware.py`](../src/admin/auth/middleware.py) |
| 2026-05-24 | **`bcrypt` используется напрямую, не через `passlib[bcrypt]`** — архитектурный принцип проекта | `passlib 1.7.4` несовместим с `bcrypt 5.0` (внутри passlib падает `password cannot be longer than 72 bytes` из-за изменения API bcrypt). `bcrypt.hashpw(secret, bcrypt.gensalt(rounds=12))` для хеширования, `bcrypt.checkpw(secret, hash)` для verify. Тот же `$2b$…` формат. `passlib[bcrypt]` будет удалён из `pyproject.toml` в Step 0 TASK-020. Альтернативы (pin `bcrypt<4.1` или ждать релиз passlib с поддержкой bcrypt 5.x) — добавляют ограничения без выигрыша | [`sessions/2026-05-24-06-task-019-review/decisions.md`](../sessions/2026-05-24-06-task-019-review/decisions.md), [`scripts/create_admin.py`](../scripts/create_admin.py) |
| 2026-05-24 | Volt Free CSS остаётся **placeholder'ом** до явной нужды в фирменном стайлинге; Bootstrap 5 + Bootstrap Icons + HTMX 2 через CDN покрывают базовую вёрстку админки | Themesberg `themesberg/volt-bootstrap-5-dashboard` в master shipping только SCSS-источники (`src/scss/volt.scss`), скомпилированный `dist/assets/css/volt.css` отсутствует. Альтернативы (npx-build на CI; другой готовый шаблон) — отложены. Триггер: реальная UX-нужда в TASK-021+ с custom-styled элементами. В BACKLOG записано «скомпилировать volt.css из SCSS» | [`sessions/2026-05-24-06-task-019-review/decisions.md`](../sessions/2026-05-24-06-task-019-review/decisions.md), [`state/BACKLOG.md`](BACKLOG.md), [`src/admin/static/css/volt.css`](../src/admin/static/css/volt.css) |
| 2026-05-24 | **Релакс инварианта `Event` через миграцию `0003`.** CHECK `ck_event_result_archive_consistency` расширен с 2 до 3 валидных комбинаций `(result_outcome_id, is_archived, archived_at)`: активное (NULL, false, NULL); архивное с итогом (NOT NULL, true, NOT NULL); **архивное без итога (NULL, true, NOT NULL)** — новый страховочный путь (TASK-018) | Принят Вариант A из 3 предложенных в [`outbox/TASK-018-question.md`](../handoff/outbox/TASK-018-question.md). B (синтетический outcome) засоряет домен; C (только UI-фильтр) противоречит task.md. Новая семантика: `is_archived = true ⇔ archived_at IS NOT NULL`; `result_outcome_id` — независим. Обновлены `docs/03-data-model.md` (инвариант) и модель `Event` (CheckConstraint) | [`sessions/2026-05-24-04-task-018-block-resolution/decisions.md`](../sessions/2026-05-24-04-task-018-block-resolution/decisions.md), [`docs/03-data-model.md`](../docs/03-data-model.md), TASK-018-amendment |
| 2026-05-24 | Cowork-агент должен сверять SQL/CHECK-фрагменты задачи с **инвариантами `docs/03-data-model.md`** до публикации | Прецедент: TASK-018 заблокирован из-за конфликта моей task-спеки с CHECK от `0001_init`. Можно было предотвратить, если бы я при проектировании прочитал «Инварианты» в `docs/03`. Запишу расширенный чек-лист в `handoff/README.md` («Проверки перед публикацией задачи») при следующем естественном касании README | [`sessions/2026-05-24-04-task-018-block-resolution/decisions.md`](../sessions/2026-05-24-04-task-018-block-resolution/decisions.md), [`handoff/README.md`](../handoff/README.md) |
| 2026-05-24 | `find_candidates` фильтрует `User.is_blocked = FALSE` — добавлено исполнителем TASK-017 по своей инициативе, принимаем | TG-API всё равно вернёт ошибку при попытке отправить заблокированному админом пользователю. Фильтр на стороне БД дешевле и не нагружает логи `TelegramForbiddenError`. Покрыто integration-тестом `test_find_candidates_blocked_user_excluded` | [`src/shared/services/reminder.py`](../src/shared/services/reminder.py), [`sessions/2026-05-24-03-task-017-review/decisions.md`](../sessions/2026-05-24-03-task-017-review/decisions.md) |
| 2026-05-24 | mypy-override `[[tool.mypy.overrides]] module = ["apscheduler.*"] ignore_missing_imports = true` в `pyproject.toml` | `apscheduler 3.x` не имеет `py.typed`. Project-wide override чище, чем `# type: ignore[import-untyped]` на каждом import. Снимется, когда у пакета появится `py.typed` | [`pyproject.toml`](../pyproject.toml) |
| 2026-05-24 | `SessionLocal` остаётся именем `async_sessionmaker` в `src/shared/db.py` (не переименовываем в `session_maker`) | Имя `SessionLocal` уже использовалось до TASK-017. Локальный CC сохранил консистентность; в моей task-спеке я писал `session_maker` по привычке, но это была моя неточность — приоритет за тем, что в коде | [`src/shared/db.py`](../src/shared/db.py) |
| 2026-05-24 | Идемпотентность scheduler-job через порядок «`record(dispatch_log)` ДО `bot.send_message`» (at-most-once) | Если send_message упадёт после record — повторно слать смысла нет (момент уже прошёл, пользователь сам зайдёт через `/events`). Если record упадёт до send_message — не отправляем, попробуем в следующем тике. UNIQUE constraint защищает от двух scheduler-инстансов | [`src/bot/scheduler/jobs.py`](../src/bot/scheduler/jobs.py) |
| 2026-05-24 | **Стилевая норма handler-сигнатур.** Все entry-point handler'ы (`cmd_*`) принимают одинаковый набор: `message`, `user`, `session` (плюс `state` для FSM-handler'ов), даже если конкретный handler не использует все параметры. `# noqa: ARG001` не добавляем — правило не enabled в ruff | Однородность упрощает чтение, тестирование (одинаковые моки) и онбординг. Минимизация параметров «на handler» — преждевременная оптимизация. Если когда-нибудь enable ARG001 — переключимся на `_user`/`_session` префиксы точечно | [`sessions/2026-05-24-02-task-016-review/decisions.md`](../sessions/2026-05-24-02-task-016-review/decisions.md), [`src/bot/routers/help.py`](../src/bot/routers/help.py) |
| 2026-05-24 | `InvalidReminderOffsetsError` получает поле `reason: Literal["too_many", "duplicate", "below_minimum"]`; handler ловит по reason, не по подстроке `str(exc)` | Текущий подстрочный match в `reminders.py._format_error` сломается тихо при правке сообщения в сервисе. Это уже проверенный паттерн — `EventNotPredictableError.reason` (TASK-009). Реализуется в Step 0 TASK-016 | [`sessions/2026-05-24-01-task-015-review/decisions.md`](../sessions/2026-05-24-01-task-015-review/decisions.md), [`src/shared/exceptions.py`](../src/shared/exceptions.py), TASK-016 Step 0 |
| 2026-05-24 | Cowork-агент перед публикацией TASK-NNN в `handoff/inbox/` обязан проверять `handoff/archive/` на наличие задачи с этим номером | Реальный кейс TASK-014: параллельный CC-инстанс через Drive backup выполнил задачу до того, как я её положил в локальный inbox. Локальному CC пришлось `git rm` дубликат. Правило `ls handoff/archive/ \| grep TASK-NNN` перед `Write` — гигиена двухмашинного workflow. Записано в `handoff/README.md` секция «Проверки перед публикацией задачи» | [`handoff/README.md`](../handoff/README.md), [`sessions/2026-05-24-01-task-015-review/decisions.md`](../sessions/2026-05-24-01-task-015-review/decisions.md) |
| 2026-05-24 | `MAX_OFFSET_MINUTES = 10080` (неделя) живёт в `src/bot/_reminders_parser.py`, не в `ReminderService` | Это UX-ограничение парсера (что пользователь может ввести через бота), не доменное правило. `ReminderService` принимает любой `int >= 5`. Если когда-то понадобится «за месяц» — правим parser, сервис не трогаем. Аналогия: telegram bot UI часто имеет более строгие ограничения, чем сервис | [`src/bot/_reminders_parser.py`](../src/bot/_reminders_parser.py), [`src/shared/services/reminder.py`](../src/shared/services/reminder.py) |
| 2026-05-23 | `render_event_card` и `event_card_kbd` принимают `back_button: tuple[str, CallbackData]` вместо `back_category_id: int \| None` | Каждая входная точка (каталог, FSM-отмена, «Мои прогнозы») собирает свою back-кнопку без знания о других сценариях. Tuple — короче и без дополнительного типа; в `NamedTuple` / `dataclass` выделим при 4-й точке использования | [`sessions/2026-05-23-13-task-014-review/decisions.md`](../sessions/2026-05-23-13-task-014-review/decisions.md), [`src/bot/routers/events.py`](../src/bot/routers/events.py) |
| 2026-05-23 | `render_event_card` принимает `allow_archived: bool = False` | Whitelist для входа из «Мои прогнозы → Архив», где архивное событие — норма. По умолчанию из каталога архивная карточка не показывается (защита от гонки админ-архивации). Дизайн чище, чем убирать guard полностью | [`src/bot/routers/events.py`](../src/bot/routers/events.py) |
| 2026-05-23 | Back-target после FSM прогноза, если зашли из «Мои прогнозы», теряется (возврат в категорию события, не в «Мои прогнозы») — оставлено как MVP, **записано в тех-долг** | Расширение трёх prediction-CB полем `back_my_tab` увеличит поверхность и риски с лимитом 64 байта callback_data. FSM-data с сериализацией требует учёта истечения state. Потеря UX редкая, пользователь сам сначала нажал «🔙 К моим прогнозам», доберётся. Триггер фикса — сигнал из UX | [`state/BACKLOG.md`](BACKLOG.md), [`src/bot/routers/prediction.py`](../src/bot/routers/prediction.py) |
| 2026-05-23 | Двухмашинный workflow через Drive проверен; pre-task cleanup PR на машине-исполнителе **может быть пустым**, если working tree чистый | TASK-014 был выполнен на удалённой машине, которая взяла задачу из Drive backup. Pre-task cleanup PR — способ синхронизации правок cowork в **текущей** рабочей копии. Чистый working tree → коммитить нечего. Ритуал `git status` всё равно обязателен | [`handoff/outbox/TASK-014-report.md`](../handoff/outbox/TASK-014-report.md), [`sessions/2026-05-23-13-task-014-review/`](../sessions/2026-05-23-13-task-014-review/) |
| 2026-05-23 | `assert user is not None` в handler'ах после `@require_active_user` остаётся | `cast(User, user)` подавляет mypy без runtime-проверки. Изменение типа в сигнатуре ломает совместимость с aiogram DI. `assert` даёт runtime-safety: если декоратор по ошибке снят, handler упадёт явным AssertionError, а не глубже через NoneType deref | [`sessions/2026-05-23-12-task-013-review/decisions.md`](../sessions/2026-05-23-12-task-013-review/decisions.md) |
| 2026-05-23 | Fallback handlers `on_predict_pick_no_state` / `on_predict_confirm_no_state` отвечают alert «Событие больше недоступно» | `edit_text + кнопка "Открыть событие заново"` — добавляет ещё один entrypoint, требует знания контекста (event_id есть в callback, но всё же). Alert минимально достаточен | [`src/bot/routers/prediction.py`](../src/bot/routers/prediction.py) |
| 2026-05-23 | `render_event_card` остаётся в `src/bot/routers/events.py` с экспортом через `__all__`, не выносится в отдельный модуль | Правило тройки: текущие 2 точки использования (`on_event` в events.py, `on_predict_cancel` в prediction.py). В TASK-014 появится 3-я (`my.py`), но это всё ещё «один экспорт». Триггер выноса в `src/bot/_event_card.py` — четвёртая точка | [`src/bot/routers/events.py`](../src/bot/routers/events.py) |
| 2026-05-23 | `on_predict_cancel` без state-фильтра — осознанный design choice | Отмена — глобальное действие пользователя. Если он жмёт «❌ Отмена» из устаревшего FSM-сообщения, бот идёт по нормальному пути: `state.clear()` + рендер карточки. Не нужен fallback с alert — нет состояния, которое можно потерять для отмены | [`src/bot/routers/prediction.py`](../src/bot/routers/prediction.py) |
| 2026-05-23 | `@require_active_user` в `cmd_predict → cmd_events` срабатывает дважды — оставляем | Двойная проверка — два `if`-сравнения на in-memory объекте, без SQL. Накладной расход незаметный. Использование `cmd_events.__wrapped__` опирается на скрытое поведение `functools.wraps` и легко сломается при следующем рефакторинге декоратора | [`src/bot/routers/prediction.py`](../src/bot/routers/prediction.py) |
| 2026-05-23 | Зеркало в Google Drive `Claude projects/Betting Bot backup` обновляется cowork-агентом через MCP Google Drive коннектор; локальный Claude Code в Drive не пишет | Источник истины — GitHub-репо; Drive — read-only онбординг-пакет для запуска локального CC на другой машине. Обязанность синхронизации на одном агенте (cowork) — избегаем рассинхрона. Папка ищется через `search_files: title = 'Betting Bot backup'`, folderId не хардкодится. См. [`handoff/README.md#зеркало-в-google-drive`](../handoff/README.md) | [`handoff/README.md`](../handoff/README.md) |
| 2026-05-23 | `_check_access(user)` inline-helper в `events.py` выносится в декоратор `@require_active_user` в `src/bot/auth.py` (Step 1 TASK-013) | Триггер — четвёртое использование шаблона (3 callback handler'а в `events.py` + минимум 4 новых в `prediction.py` TASK-013). Правило тройки соблюдено. Декоратор предпочтительнее middleware: явнее в сигнатуре handler'а (`user: User` без `\| None`), не требует регистрации на router, разруливает Message vs CallbackQuery через isinstance внутри одной функции | [`sessions/2026-05-23-11-task-012-review/decisions.md`](../sessions/2026-05-23-11-task-012-review/decisions.md), TASK-013 Step 1 |
| 2026-05-23 | `EventService.list_categories_with_counts` остаётся в `EventService`, не переезжает в `CategoryService` | `EventService` уже композирует Event/Outcome/Prediction/AuditLog; импорт модели `Category` для одного метода с outer join + `count FILTER` — мелочь по сравнению с инъекцией `EventRepository` в `CategoryService`. Один SQL логически принадлежит «событийной» статистике, не каталогу категорий | [`src/shared/services/event.py`](../src/shared/services/event.py) |
| 2026-05-23 | `PAGE_SIZE = 7` остаётся локальной константой в `src/bot/routers/events.py` | Правило тройки: одна константа в одном файле не оправдывает создание `src/bot/_consts.py`. В TASK-013 пагинации нет; триггер для выноса — TASK-014 «Мои прогнозы» с собственной пагинацией | [`src/bot/routers/events.py`](../src/bot/routers/events.py), [`state/BACKLOG.md`](BACKLOG.md) |
| 2026-05-23 | UTC-форматирование datetime в карточке события без локализации — оставлено на MVP | TZ-инфраструктура требует решения по источнику (явный выбор пользователя? IP? UTC по умолчанию?) и формату (день недели? относительные «через 2 часа»?). Откладывается до отдельной задачи после согласования | [`src/bot/texts.py`](../src/bot/texts.py), [`docs/04-bot-flows.md`](../docs/04-bot-flows.md) |
| 2026-05-23 | Phone-нормализация (телеграмовский формат → E.164) делается в bot-handler'е, не в сервисе | Сервис принимает E.164 как контракт; нормализация — форматирование входа на границе с внешним миром. Иначе сервис не сможет полагаться на свой вход. Когда в админке появится ручной ввод телефона — там будет своя нормализация (с валидацией пробелов/скобок/дефисов) | [`src/bot/routers/start.py`](../src/bot/routers/start.py) |
| 2026-05-23 | `_phone_hash` дублируется между `start.py` и `http_registry.py` — оставляем до 3-го места использования | «Правило тройки»: одна строка в двух местах не оправдывает создание `src/shared/utils.py`. Когда хелпер понадобится в третьем модуле — вынесем явно | [`src/bot/routers/start.py`](../src/bot/routers/start.py), [`src/shared/external/http_registry.py`](../src/shared/external/http_registry.py) |
| 2026-05-23 | `RegistryUnavailableError` логируется как `warning`, не `info` | Деградация внешнего сервиса — это сигнал, на который оператор должен реагировать (даже если для одного пользователя ничего катастрофического). `info` был бы скрыт в production-логах с фильтром по WARN+ | [`src/bot/routers/start.py`](../src/bot/routers/start.py) |
| 2026-05-23 | `WELCOME_NEW_REGISTERED.format(first_name=...)` — допустимо | Теоретическая уязвимость к фигурным скобкам в имени; на практике Telegram не пропускает `{` в `first_name`. Шаблон с placeholder сохраняет i18n-friendly стиль (будущая локализация подменяет строку, не код) | [`src/bot/texts.py`](../src/bot/texts.py), [`src/bot/routers/start.py`](../src/bot/routers/start.py) |
| 2026-05-23 | Narrowing-паттерн `if message.contact is None or message.from_user is None: return` в `F.contact` handler'е | mypy strict не «понимает» aiogram-фильтры. Явное if-return — стандартный и читаемый способ убедить тайп-чекер; альтернатива `assert` валит handler с AssertionError в проде вместо корректного return | [`src/bot/routers/start.py`](../src/bot/routers/start.py) |
| 2026-05-23 | `src/__init__.py` создан — `src` обычный пакет (не namespace); все импорты в репо абсолютные `from src.*` | Без `src/__init__.py` mypy ругается «одно имя через разные пути» (`from src.shared.logging` в bot/main vs `from .logging` в shared/__init__). С пакетом одно каноническое имя. Правило выписано в `docs/08-conventions.md` («`src/` — полноценный пакет, не namespace») | [`docs/08-conventions.md`](../docs/08-conventions.md), [`handoff/outbox/TASK-010-report.md`](../handoff/outbox/TASK-010-report.md) |
| 2026-05-23 | Throttling `UserMiddleware.touch_last_seen` отложен — MVP-долг | Сейчас на каждый update идёт SELECT + UPDATE + COMMIT. Под нагрузкой превратится в bottleneck; решение — Redis-кеш «не чаще раза в N минут». Триггер для реализации — реальные метрики. Записано в [`BACKLOG.md`](BACKLOG.md) раздел «Технический долг» | [`state/BACKLOG.md`](BACKLOG.md), [`src/bot/middlewares/user.py`](../src/bot/middlewares/user.py) |
| 2026-05-23 | Фабрики/сборочные функции читают конфиг через `get_settings()`, не через module-level `settings` | Module-level `settings` зафиксирован на момент импорта; `get_settings.cache_clear()` в тестах его не подхватит. Фабрики — `build_dispatcher`, `get_registry_client` — должны видеть свежий env. Хендлеры/сервисы могут использовать module-level (singleton, перечитывать не нужно). Правило в `docs/08-conventions.md` («Фабрики читают свежий конфиг») | [`docs/08-conventions.md`](../docs/08-conventions.md) |
| 2026-05-23 | `structlog.contextvars.clear_contextvars()` в finally `LoggingMiddleware` — оставить как есть | Поведение корректное (Python `contextvars` per asyncio.Task), clear — defensive guarantee. Изменений не требуется | [`src/bot/middlewares/logging.py`](../src/bot/middlewares/logging.py) |
| 2026-05-23 | `TELEGRAM_BOT_TOKEN = "111111:stub-token"` в `tests/unit/conftest.py` — стабильная test-fixture | Валидный для aiogram формат, deterministic. CI workflow `env:` для unit job отсутствует (узкий до tests/unit/); конфликтов нет | [`tests/unit/conftest.py`](../tests/unit/conftest.py) |
| 2026-05-23 | Импорты внешних модулей: type-only — из подмодуля, чтобы не тригерить side-effects пакетного `__init__.py` (фабрики, `lru_cache`) | `from ..external import ExternalUserRegistryClient` при импорте дёрнет `get_registry_client()` и тронет `Settings()`. Для типа в аннотации правильно `from ..external.registry import ExternalUserRegistryClient`. Правило дописано в `docs/08-conventions.md` | [`docs/08-conventions.md`](../docs/08-conventions.md) |
| 2026-05-23 | Раздельные fixture'ы: `session` (rollback) для repository-тестов, `nested_session` (SAVEPOINT) для service-тестов | Унификация на `nested_session` дала бы ~1мс overhead per test, но потеряла бы выразительность intent'а («этот тест не коммитит — этому коммит важен»). Комментарий с when-use-which пойдёт в `tests/integration/conftest.py` через Step 0 TASK-010 | [`handoff/outbox/TASK-009-report.md`](../handoff/outbox/TASK-009-report.md), TASK-010 Step 0 |
| 2026-05-23 | Сервисы **не** делают `await session.rollback()` на доменных исключениях | Сессия — контекст-менеджер middleware/handler, который сам откатит при пробросе исключения. Сервисный rollback избыточен и конфликтует с SAVEPOINT-фикстурами тестов. Конвенция уточнена в `docs/08-conventions.md` (правило #3). Точечный фикс `EventService.delete_outcome` в Step 0 TASK-010 | [`docs/08-conventions.md`](../docs/08-conventions.md), TASK-010 Step 0 |
| 2026-05-23 | `UserService.touch_last_seen` остаётся с commit (как все write-методы сервисов) | Консистентность важнее микрооптимизации; throttle (обновлять только если `last_seen_at` старше N минут) — отдельная задача, когда увидим реальную нагрузку | [`src/shared/services/user.py`](../src/shared/services/user.py) |
| 2026-05-23 | `StubRegistry` остаётся в `tests/integration/services/conftest.py` | Используется только UserService-тестами; переезд в `tests/_stubs.py` имеет смысл только при появлении unit-тестов с замоканными репо — пока их нет | [`tests/integration/services/conftest.py`](../tests/integration/services/conftest.py) |
| 2026-05-23 | `X-Request-Id` общий на все retry-попытки одного `verify(phone)` (не свежий на каждую) | Один логический запрос — один ID; если внешний API использует ID для идемпотентности (типичный паттерн), ретраи с тем же ID помогут серверу детектировать дубли; различение попыток уже даёт `retry_count` в логах. Реализуется в TASK-009 Step 0 | TASK-009 (Step 0), [`handoff/outbox/TASK-008-report.md`](../handoff/outbox/TASK-008-report.md) |
| 2026-05-23 | `get_registry_client()` — singleton через `@lru_cache(maxsize=1)` | HTTP connection pool теряет смысл без singleton; на каждый вызов новый `httpx.AsyncClient` — пустая трата. Тесты делают `cache_clear()` как с `get_settings()`. Шатдаун консьюмеры дёрнут `await client.close()`. Реализуется в TASK-009 Step 0 | TASK-009 (Step 0), [`src/shared/external/__init__.py`](../src/shared/external/__init__.py) |
| 2026-05-23 | `load_mock_config` остаётся defensive (невалидный YAML → пустой конфиг) | Mock — dev/test инфра, молчаливый no-op обнаружится в первом же `verify()` вызове. Pydantic-валидатор — лишняя ceremony без выигрыша | [`src/shared/external/mock_registry.py`](../src/shared/external/mock_registry.py) |
| 2026-05-23 | `MockExternalUserRegistryClient.fail_rate` использует глобальный `random` | Тесты используют 0.0/1.0 (детерминировано); сценарий «30% fail воспроизводимо» сейчас не нужен. Добавить `Random` параметр сможем позже, когда понадобится | [`src/shared/external/mock_registry.py`](../src/shared/external/mock_registry.py) |
| 2026-05-23 | `HttpExternalUserRegistryClient.client: AsyncClient \| None` — допустимый паттерн | Идиоматический httpx-паттерн для DI тестов с `MockTransport`. Factory-of-client добавил бы индирекцию без выигрыша | [`src/shared/external/http_registry.py`](../src/shared/external/http_registry.py) |
| 2026-05-23 | В integration-тестах — per-test engine с `NullPool` (а не общий `src.shared.db.engine`) | `src.shared.db.engine` — module-level singleton, привязан к первому event loop'у; pytest-asyncio даёт каждому тесту свой loop → `RuntimeError: Event loop is closed` на втором тесте. Per-test engine ~1мс накладной расход, но даёт чистую изоляцию | [`tests/integration/conftest.py`](../tests/integration/conftest.py), [`handoff/outbox/TASK-007-report.md`](../handoff/outbox/TASK-007-report.md) |
| 2026-05-23 | После `pg_insert + on_conflict_do_update` репозиторий вызывает `session.refresh(obj)` | Identity_map SQLAlchemy после повторного upsert возвращает закэшированный экземпляр со старыми значениями. `refresh` стоит один SELECT (не на горячем пути) и надёжнее, чем `session.expire(obj, [fields...])` с ручной синхронизацией списка полей | [`src/shared/repositories/prediction.py`](../src/shared/repositories/prediction.py), [`src/shared/repositories/reminder_setting.py`](../src/shared/repositories/reminder_setting.py) |
| 2026-05-23 | CI unit job сужен до `tests/unit/` (не `tests/`) | pytest при collection импортирует все файлы в `testpaths`; integration-тесты тянут `src.shared.repositories` → `src.shared.config` → `Settings()`, что без stub-env падает. Расширение workflow-env'а для unit job — мусор; чистое разделение по директориям естественнее | [`.github/workflows/ci.yml`](../.github/workflows/ci.yml) |
| 2026-05-23 | `EventRepository.create()` ставит `{}` в `metadata_`, если передан `None` | Дублирование с `server_default='{}'::jsonb`, но делает in-memory объект сразу пригодным к использованию без `refresh()`. Server_default — safety net для прямого SQL мимо репозитория | [`src/shared/repositories/event.py`](../src/shared/repositories/event.py) |
| 2026-05-23 | `AuditLogRepository.list_with_admin(selectinload(admin))` отложен до TASK-026 | Сейчас не видим реальный pattern доступа в админке. YAGNI — добавим, когда будет ясно, нужно ли подгружать `admin.full_name` или достаточно `admin_id` | [`src/shared/repositories/audit_log.py`](../src/shared/repositories/audit_log.py) |
| 2026-05-23 | Порядок задач: TASK-008 = внешний registry (interface + mock), TASK-009 = сервисы (бывшие 009 и 008 поменялись местами) | `UserService.register_or_authenticate` дёргает `ExternalUserRegistryClient.verify(phone)` — сервисы зависят от интерфейса реестра. Реестр должен идти первым; иначе UserService писался бы без реальной depency и переделывался | [`state/BACKLOG.md`](BACKLOG.md), TASK-008 |
| 2026-05-23 | `tests/conftest.py` → `tests/unit/conftest.py` (stub-env только для unit) | Module-level `os.environ.setdefault` перетирал `DATABASE_URL` integration-тестов. Перенос даёт чистое разделение: unit-тесты получают stub-env, integration — реальный (из `.env` локально или `env:`-блока в CI workflow) | [`handoff/outbox/TASK-006-report.md`](../handoff/outbox/TASK-006-report.md) |
| 2026-05-23 | Integration-тесты дёргают `alembic` через `subprocess uv run alembic ...`, не in-process `alembic.command.*` | Тестируем ту же CLI-поверхность, которую использует `make migrate`. In-process переиспользовал бы event loop тестов — потенциальная путаница с async engine. 3.5с на 4 теста — приемлемо для миграций | [`tests/integration/test_migrations.py`](../tests/integration/test_migrations.py) |
| 2026-05-23 | `engine` в `src/shared/db.py` — module-level constant, не lazy `get_engine()` | Все пути импорта защищены conftest'ом (unit) или прямым env (integration/prod). Lazy добавил бы две лестницы (config + engine), которые редко нужны. Если позже появится тест, который импортирует `db` без env, — починим точечно | [`src/shared/db.py`](../src/shared/db.py) |
| 2026-05-23 | Мини-парсер `.env` в `tests/integration/test_migrations.py` (вместо `python-dotenv`) | 10 строк против отдельной зависимости; используется только здесь. Если потребуется ещё где-то, добавим `python-dotenv` к dev-deps | [`tests/integration/test_migrations.py`](../tests/integration/test_migrations.py) |
| 2026-05-23 | В CI job `integration` нет explicit teardown postgres-service | Service-container умирает вместе с runner'ом, GitHub Actions сам всё снесёт. `if: always()` cleanup имеет смысл только для внешних managed-ресурсов | [`.github/workflows/ci.yml`](../.github/workflows/ci.yml) |
| 2026-05-23 | Бывший пункт BACKLOG «репозитории/сервисы заготовки» разделён на TASK-007 (только repos) и TASK-008 (только services) | Каждая задача получает чёткий фокус и тестируемую границу. Repos — query-слой без транзакций, services — композиция и транзакции с доменными исключениями | [`state/BACKLOG.md`](BACKLOG.md), TASK-007 |
| 2026-05-23 | `Event.metadata_` — `NOT NULL` с `server_default='{}'` (вместо nullable) | Стандарт для JSONB-«bag of attributes»: код не делает None-проверок, JSON-операторы (`||`, `->`, `?`) работают без приведений; пустое значение однозначно представлено как `{}` | [`handoff/outbox/TASK-005-report.md`](../handoff/outbox/TASK-005-report.md), TASK-006 (Step 0) |
| 2026-05-23 | `AdminUser.full_name` — `String(128)` (вместо `Text`) | Консистентно с `User.first_name`/`last_name` (`String(64)`). `Text` не даёт выигрыша в Postgres, но создаёт расхождение в стиле | [`handoff/outbox/TASK-005-report.md`](../handoff/outbox/TASK-005-report.md), TASK-006 (Step 0) |
| 2026-05-23 | Naming convention для `ck` сменена на `%(constraint_name)s` (без авто-префикса); в моделях `CheckConstraint(name="ck_event_...")` пишется полным именем | Унификация со стилем `uq`/`fk`/`ix`, где явное `name=` перекрывает convention. После — везде полные имена; grep по имени constraint находит и модель, и тест | [`handoff/outbox/TASK-005-report.md`](../handoff/outbox/TASK-005-report.md), TASK-006 (Step 0) |
| 2026-05-23 | `Prediction.is_correct` — без `server_default`; PG-дефолт NULL остаётся неявным | Явный `server_default=null()` не меняет поведения, только добавляет код | [`handoff/outbox/TASK-005-report.md`](../handoff/outbox/TASK-005-report.md) |
| 2026-05-23 | `Outcome.event_id` — `ondelete="CASCADE"` остаётся | Hard-delete события возможен только при отсутствии прогнозов (`Prediction.event_id` → RESTRICT); тогда orphan-исходы корректно стираются. Целостность гарантирована | [`handoff/outbox/TASK-005-report.md`](../handoff/outbox/TASK-005-report.md) |
| 2026-05-23 | В `Settings` (pydantic-settings) — `extra="ignore"`, не `"forbid"` | `.env` содержит `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` для compose-подстановки, эти ключи не поля приложения. С `forbid` каждое чтение `Settings()` ругалось бы; объявлять их pass-through полями — холостая работа (вся инфа уже в `DATABASE_URL`) | [`src/shared/config.py`](../src/shared/config.py), [`handoff/outbox/TASK-004-report.md`](../handoff/outbox/TASK-004-report.md) |
| 2026-05-23 | Тесты конфига — через `monkeypatch.setenv`, не `_env_file=tmp` | `conftest.py` setdefault'ит stub-env на module-level (иначе `from src.shared import ...` падает в CI без `.env`); этот stub перекрывает `_env_file`. `monkeypatch.setenv` — стандартный pytest-паттерн, чище контролируется. Сам conftest позже (в TASK-006) переехал в `tests/unit/conftest.py` — см. строку про tests/unit/conftest.py выше | [`tests/unit/test_config.py`](../tests/unit/test_config.py), [`tests/unit/conftest.py`](../tests/unit/conftest.py) |
| 2026-05-23 | Приоритет источников конфига: `os.environ` > `.env`-файл (дефолт pydantic-settings) | Соответствует ожиданиям всей экосистемы (docker, k8s, systemd). Кастомизация `customise_sources` не делается — добавила бы сложности без выигрыша | [`src/shared/config.py`](../src/shared/config.py) |
| 2026-05-23 | Раскладка compose-файлов: база + `override.yml` (dev, в репо) + `prod.yml` (явный `-f`) | Стандартная схема compose v2. Минимум дублирования, понятная инверсия (dev — авто, prod — явный). Реализация поэтапно: сейчас только база (db+redis); bot/web/override/prod — в TASK-026 | [`docs/07-deployment.md`](../docs/07-deployment.md), [`infra/docker-compose.yml`](../infra/docker-compose.yml) |
| 2026-05-23 | `.env.example` показывает оба варианта `DATABASE_URL` / `REDIS_URL` (localhost / compose-имена) | На текущем этапе приложение запускается с хоста, БД в compose — нужен `localhost`. После TASK-026 в compose будут служебные имена. Документируем оба, по умолчанию — dev (localhost) | [`infra/.env.example`](../infra/.env.example) |
| 2026-05-23 | `make nuke` подтверждает ввод буквами `NUKE` | Альтернативы `y/yes` / `--force` отвергнуты как недостаточно осознанные. `DELETE` путается с SQL | [`Makefile`](../Makefile) |
| 2026-05-23 | Pre-task cleanup PR — штатная часть workflow | После TASK-002 локальный агент сам применил паттерн (PR #1): накопившиеся правки cowork (state/docs/handoff/конфиги) оформляются отдельным PR перед основной фичей. Это держит diff фичи минимальным | [`handoff/README.md#pre-task-cleanup-pr`](../handoff/README.md#pre-task-cleanup-pr), [`sessions/2026-05-23-01-task-002-review/decisions.md`](../sessions/2026-05-23-01-task-002-review/decisions.md) |
| 2026-05-23 | CI триггеры сужены: `push: branches: [main]` + `pull_request: branches: [main]` | Раньше push'ы в feature-ветки + сами PR давали дублирующие прогоны. Сузили, чтобы не платить минуты дважды; обратная связь по фиче приходит на момент PR, что совпадает с workflow handoff | [`.github/workflows/ci.yml`](../.github/workflows/ci.yml) |
| 2026-05-23 | Репо — сервис, не библиотека: нет `[build-system]`, `[tool.uv] package = false`, `src.*` через `PYTHONPATH` | Решение принято локальным агентом в TASK-002 и формализовано постфактум. Wheel не собираем, в Docker делаем `COPY src /app/src` + `PYTHONPATH=/app`. Влияет на TASK-003+ (Dockerfile-ы) | [ADR-0004](../docs/adr/0004-no-build-backend.md), [`handoff/outbox/TASK-002-report.md`](../handoff/outbox/TASK-002-report.md) |
| 2026-05-22 | Branch protection для `main` отложен | GitHub free не поддерживает protection на private-репозиториях. Альтернативы (public / Pro) отклонены. Митигация — дисциплина workflow: пушит только локальный Claude Code, ему явно запрещено `CLAUDE.md` пушить в `main` помимо PR | [`handoff/outbox/TASK-001-report.md`](../handoff/outbox/TASK-001-report.md), [`CLAUDE.md`](../CLAUDE.md) |
| 2026-05-22 | Имя репозитория и пакета — `bettgbot` | Репо уже было создано владельцем под этим именем; для консистентности приведено в `pyproject.toml` и docs (`docs/02-tech-stack.md`, `docs/07-deployment.md`). Имя продукта в текстах остаётся «Betting Bot» | [`pyproject.toml`](../pyproject.toml) |
| 2026-05-22 | Секреты для git/GitHub — в macOS Keychain, доступ через `gh auth git-credential` | PAT хранится в Keychain (internet-password, server `github.com`); `gh auth setup-git` прописал credential helper. Отдельный ADR не заводим — это факт о среде владельца, не архитектурное решение проекта | [`handoff/outbox/TASK-001-report.md`](../handoff/outbox/TASK-001-report.md) |
| 2026-05-22 | Двухагентная схема: cowork-агент проектирует, локальный Claude Code исполняет через `handoff/` | Длинные сессии в cowork невозможны; нужно сохранять контекст между сессиями и иметь возможность пушить локально | [ADR-0003](../docs/adr/0003-handoff-protocol.md) |
| 2026-05-22 | Стек: Python 3.12 + aiogram 3 + FastAPI + SQLAlchemy 2 + PostgreSQL 16 + Redis + Docker Compose | Несколько тысяч пользователей, редкие события, нужна транзакционность и расширяемость | [ADR-0001](../docs/adr/0001-tech-stack.md), [`docs/02-tech-stack.md`](../docs/02-tech-stack.md) |
| 2026-05-22 | Monorepo: `bot`, `admin`, `shared`, миграции в одном репо | Общие модели и репозитории; одна команда деплоя | [ADR-0002](../docs/adr/0002-monorepo-layout.md) |
| 2026-05-22 | Внешний API проверки пользователей — mock-адаптер на этапе разработки; интерфейс `ExternalUserRegistryClient` готов к замене | Реальный API ждёт согласования контракта с владельцем системы | [`docs/06-external-api.md`](../docs/06-external-api.md) |
| 2026-05-22 | Архивация событий — мягкая (флаг `is_archived` + ts), не удаление; прогнозы остаются связанными | Нужна статистика и пересмотр; админ должен видеть полную историю | [`docs/03-data-model.md`](../docs/03-data-model.md) |
| 2026-05-22 | UI админки — готовый Bootstrap 5 шаблон (выбор конкретного — в момент реализации, см. TASK-018) | Своего дизайна нет; цель — минимальная функциональная админка | [`docs/05-admin-spec.md`](../docs/05-admin-spec.md) |
| 2026-05-22 | Регистрация пользователя — только через `Contact` Telegram, без ручного ввода телефона | Согласие на обработку + защита от опечаток + Telegram уже верифицировал номер при регистрации | [`docs/04-bot-flows.md`](../docs/04-bot-flows.md) |
| 2026-05-22 | FSM-storage — Redis, не in-memory | Для перезапусков бота без потери состояния пользователя; задел под несколько инстансов | [`docs/02-tech-stack.md`](../docs/02-tech-stack.md) |

## Как добавлять записи

- Новая строка сверху (последнее решение — первым).
- Если решение крупное (затрагивает архитектуру, стек, безопасность, контракт с внешними системами) — оно **дополнительно** оформляется как ADR в `docs/adr/`, и в столбце «Где подробнее» появляется ссылка.
- Если решение позже отменено — старая строка не удаляется, добавляется новая с пометкой «отменяет от YYYY-MM-DD».
