# Решения — task-018-block-resolution

| # | Решение | Альтернативы | Обоснование |
|---|---|---|---|
| 1 | **Вариант A: релакс CHECK `ck_event_result_archive_consistency`** через миграцию `0003`, расширяющую инвариант до 3 валидных комбинаций | B — синтетический outcome; C — только UI-фильтр | A минимально расширяет data-model, явно фиксирует «два пути архивации», семантика чистая (`is_archived ⇔ archived_at IS NOT NULL`). B засоряет домен outcomes и ломает `mark_correctness`. C противоречит task.md и требует дублирование условий в каждом «активные события» запросе |
| 2 | Новая семантика инварианта: **3 допустимые комбинации `(result_outcome_id, is_archived, archived_at)`** | Сохранить старый «2 комбинации» CHECK | Активное (NULL, false, NULL) + архивное с итогом (NOT NULL, true, NOT NULL) + **архивное без итога (NULL, true, NOT NULL)** — последняя описана в `docs/04-bot-flows.md` как «страховочный путь» |
| 3 | `downgrade()` миграции 0003 **не пытается auto-fix данные** (падает при наличии архивных без итога) | Auto-rollback на «is_archived=false для архивных без result_outcome_id» | Бизнес-логика «что делать с забытыми событиями» — не техническая. Если оператор делает downgrade — пусть осознанно решит. Caveat в docstring downgrade'а |
| 4 | Обновление `docs/03-data-model.md` — **зона cowork**, делаю сам, не делегирую локальному CC | Делегировать локальному CC в TASK-018 («доработай docs под новый инвариант») | `CLAUDE.md` запрещает локальному CC править `docs/` без явного указания. Расширение инварианта data-model'и — архитектурное решение, должно жить в зоне проектировщика |
| 5 | Cowork-агент должен сверять SQL-фрагменты задачи с CHECK-constraint'ами из `docs/03-data-model.md` **до публикации задачи** | Полагаться на исполнителя | Этот блокер можно было предотвратить, если бы я при проектировании TASK-018 прочитал «Инварианты» в `docs/03-data-model.md`. Записать в `handoff/README.md` секция «Проверки перед публикацией задачи» (уже есть от TASK-015, расширяем) |
| 6 | Пометка в моей дальнейшей работе: **расширить «Проверки перед публикацией задачи»** в `handoff/README.md` после следующего успешного цикла (не сейчас, чтобы не плодить cleanup-PR'ов) | Сейчас же | Минимизация cleanup-итераций. Запишу в TASK-019 (если он будет) или в начало Этапа 3 |
