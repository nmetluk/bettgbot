# Решения — task-007-review

| # | Решение | Альтернативы | Обоснование | Где зафиксировано |
|---|---|---|---|---|
| 1 | В integration-тестах — per-test engine с `NullPool` | (а) session-scoped event loop в pytest-asyncio; (б) lazy/per-loop `src.shared.db.engine` | (а) даёт другие side-эффекты (async-generator фикстуры начинают вести себя странно), (б) усложняет `db.py` ради тестов. Текущее решение изолированное, накладной расход ~1мс на тест | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`tests/integration/conftest.py`](../../tests/integration/conftest.py) |
| 2 | `session.refresh(obj)` после `pg_insert + on_conflict_do_update + RETURNING *` | `session.expire(obj, ["fields..."])` — экономия одного round-trip | Upsert не на горячем пути; `refresh` проще и не требует синхронизации списка полей с моделью | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`src/shared/repositories/prediction.py`](../../src/shared/repositories/prediction.py) |
| 3 | CI unit job сужен до `tests/unit/` | Расширить workflow `env:` блок stub-значениями для unit job (как у integration) | Unit job не должен пытаться импортировать integration-тесты, для которых нет реальной БД. Чистое разделение по директориям выразительнее | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) |
| 4 | `EventRepository.create()` ставит `metadata_={}` для `None` | Убрать и полагаться на `server_default='{}'::jsonb` | Дублирование подстраховочное и UX-полезное: in-memory объект сразу пригоден к `event.metadata_` без `refresh()` | [`state/DECISIONS.md`](../../state/DECISIONS.md), [`src/shared/repositories/event.py`](../../src/shared/repositories/event.py) |
| 5 | `AuditLogRepository.list_with_admin` (с `selectinload(admin)`) отложен до TASK-026 | Добавить сейчас впрок | Пока не видим реальный pattern доступа в админке (рендерится `entry.admin.full_name` или достаточно `admin_id`). YAGNI | [`state/DECISIONS.md`](../../state/DECISIONS.md) |
| 6 | Перестановка задач: TASK-008 = внешний registry, TASK-009 = сервисы (бывшие 009 и 008) | Оставить порядок как было (сервисы → потом registry) | `UserService.register_or_authenticate` (см. sequence в [`docs/01-architecture.md`](../../docs/01-architecture.md)) дёргает `ExternalUserRegistryClient.verify(phone)`. Сервисы зависят от интерфейса реестра — реестр должен идти первым, иначе UserService писался бы с заглушкой и переделывался | [`state/BACKLOG.md`](../../state/BACKLOG.md), TASK-008 |
