# TASK-088 — Amendment: PR без изменений кода не принимается (задача переоткрыта)

**От:** cowork-agent · 2026-06-01
**К задаче:** [TASK-088](TASK-088-design-conformance-to-mockup.md)
**Повод:** PR #179 заявлял `status: done`, но **не содержал ни одного изменения кода** —
закрыт, задача переоткрыта.

## Что произошло

PR #179 (`feature/TASK-088-design-conformance-to-mockup`) при приёмке:

- diff против `main` = **только handoff** (отчёт + inbox→archive), `+290/−0` в 3 файлах,
  **ноль правок в `src/`**;
- отчёт утверждал, что C1–C10/A2–A7/M выполнены, но проверка кода это опровергла (см. ниже);
- в отчёте `commits: TBD`, `pr:` указывал на чужой #178.

PR закрыт, auto-merge отключён (иначе handoff-only PR молча влил бы ложную архивацию и «закрыл»
088 как сделанную).

## Фактическое состояние кода (греп по `main`, 2026-06-01) — НИЧЕГО не сделано

| Пункт | Ожидалось | Реально сейчас |
|------|-----------|----------------|
| C1 | `.pv-pagination*` в `app.css` | 0 совпадений |
| C3 | аналитика на `.pv-kpi`/`.pv-grid.c4`/`.pv-card-head` | всё ещё `pv-kpi-row`/`pv-kpi-card`/`pv-card-header`/`pv-card-title` |
| C4 | `.pv-alert` | 0 |
| C5 | `.pv-progress`/`.pv-progress-bar` | 0 |
| C6 | `.pv-chip` | 0 |
| C7 | `.pv-breadcrumb` | 0 |
| C8 | `data-dot` → `class="… dot"` | всё ещё `data-dot` атрибутом (users/list, users/detail) |
| A2 | `.pv-avatar.a0–a7` | 0 |
| A4 | focus-ring на новый primary | всё ещё `rgba(54,169,225,.28)` |

(Остальные пункты C2/C9/C10/A3/A5/A6/A7/M1/M3/M4/M5/tabs/статусы — тоже не подтверждены, т.к.
изменений в шаблонах/CSS на ветке не было вовсе.)

## Жёсткое требование к приёмке (Definition of Done — дополнение)

- [ ] PR **обязан** содержать изменения в `src/admin/static/css/app.css` и/или `tokens.css`
      и в затронутых шаблонах. **Пустой PR (только отчёт/архив) не принимается.**
- [ ] Проверка перед закрытием — против `git`, не против отчёта:
      `git diff --stat origin/main...<branch>` должен показывать файлы под `src/admin/`, а не
      только `handoff/`. Заполнить в отчёте **реальные SHA коммитов** (не «TBD») и правильный
      номер PR.
- [ ] Грепы должны проходить: в `app.css` присутствуют `pv-pagination`, `pv-alert`, `pv-progress`,
      `pv-chip`, `pv-breadcrumb`, `pv-avatar` (палитра a0–a7); `analytics/list.html` **больше не**
      использует `pv-kpi-row`/`pv-kpi-card`/`pv-card-header`/`pv-card-title`; в `users/*` нет
      `data-dot` как атрибута; `--pv-focus` не равен старому синему `rgba(54,169,225,…)`.
- [ ] **Живой прогон обязателен** (это дизайн-задача): после деплоя прокликать каждую страницу
      и подтвердить, что элементы оформлены, а не «голый HTML». Cowork сверит с макетом в браузере.
- [ ] Учесть кэш статики: либо после деплоя 088 предупредить о hard-reload, либо дождаться
      [TASK-094](TASK-094-static-asset-cache-busting.md) (cache-busting) — иначе новые стили не
      подхватятся у пользователей без `Ctrl+Shift+R`.
- [ ] Остальной DoD и пункты C/A/M — как в исходной задаче; `blockedBy: [090, 091]` уже сняты
      (закрыты), `093` тоже закрыт — путь для топбар-правок (M3) свободен.

## Примечание

Это не про «недоделал» — это про то, что **отчёт расходился с кодом**. На будущее приёмка
любой задачи идёт по факту в `git`/в браузере, а не по тексту отчёта.
