# TASK-088 — Amendment 2: остаток C3 (один заголовок аналитики) + переоткрыть PR

**От:** cowork-agent · 2026-06-01
**К задаче:** [TASK-088](TASK-088-design-conformance-to-mockup.md)
**Контекст:** после Amendment 1 исполнитель добавил реальный код (коммит `6b73f9d`
`feat(admin): bring UI in full conformance…`). Приёмка по `git` — **в целом сделано**: см. ниже.

## Что проверено и подтверждено (грепом по ветке)

- `app.css`: `pv-pagination`, `pv-alert`, `pv-progress`, `pv-chip`, `pv-breadcrumb`,
  аватары `pv-avatar.a0–a7` (A2) — присутствуют.
- C8: `data-dot` как атрибут убран (`users/list`, `users/detail`).
- A4: `--pv-focus` синхронизирован на новый primary (`rgba(40,126,168,.28)`), правки TASK-072 сохранены.
- Статусы события: «архив» → `info`, «закрыт» → `warning`.
- 093 не сломан: `@click` со скобками в шаблонах нет.

## Остаток (нужно поправить — High, мелочь)

**C3 — недоконвертирована одна карточка аналитики.** В
`src/admin/templates/analytics/list.html`, строки **102–103**:

```jinja
<div class="pv-card-header">
    <h2 class="pv-card-title">Топ-10 событий по числу прогнозов</h2>
</div>
```

Классы `pv-card-header` / `pv-card-title` в `app.css` **не определены** (введён `pv-card-head`).
Первые две карточки аналитики переведены на `pv-card-head` правильно — эту пропустили → её
заголовок отрендерится голым `<h2>` (тот же дефект C3, но в одной секции).

- [ ] Заменить на паттерн остальных карточек: `pv-card-head` + `<h3>` (как в строках ~34/52).
- [ ] Греп-проверка: `grep -n "pv-card-header\|pv-card-title" src/admin/templates/` → пусто.

## Процесс (важно)

- [ ] **Переоткрыть PR #179** (он был закрыт cowork'ом, когда не содержал кода). Сейчас в ветке
      есть коммит `6b73f9d` — нажать **Reopen**, либо открыть новый PR с этой же ветки. Пока PR
      закрыт — изменения не вольются.
- [ ] В отчёте проставить **реальные SHA** коммитов и правильный номер PR (в прошлой версии было
      `commits: TBD` и чужой `#178`).
- [ ] Деплой/кэш: 088 — крупный CSS-деплой. Чтобы пользователи увидели стили без `Ctrl+Shift+R`,
      выкатить [TASK-094](TASK-094-static-asset-cache-busting.md) **до/вместе** с 088 или
      предупредить о hard-reload.

## Приёмка

После фикса строки 102–103, переоткрытия PR и деплоя — cowork делает **живой прогон по всем
страницам** (это финальная приёмка дизайн-задачи: верстка сверяется с макетом в браузере, а не
по отчёту).
