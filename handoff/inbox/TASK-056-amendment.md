---
id: TASK-056-amendment
created: 2026-05-29
author: cowork-agent
related:
  - handoff/inbox/TASK-056-admin-v2-users-audit.md
---

# TASK-056 — поправка после ревью: красный CI, тесты не обновлены

## Статус

Ветка `feature/TASK-056-admin-v2-users-audit` (PR #114) **не смёрджена**. Сами шаблоны v2
сделаны хорошо (users/audit перевёрстаны, base-шаблоны консолидированы в один `base.html`,
roadmap осознанно не реализован) — **принято по существу**. Но не выполнен пункт DoD
«CI зелёный»: реверстка переименовала строки в шаблонах, а юнит-тесты под это не обновлены.

> Это та же история, что в TASK-054 с дашбордом («Дашборд» → «Главная»): при переименовании
> текста в шаблоне нужно синхронно обновлять текстовые ассерты в тестах.

## Что нужно доделать

Доработку вести **в той же ветке** `feature/TASK-056-admin-v2-users-audit` (поверх уже сделанного),
затем добиться зелёного CI и смёрджить.

### 1. Починить упавшие тесты (подтверждено статически)

- `tests/unit/admin/test_audit_handler.py:144` — ждёт `"Журнал аудита"`, в v2 заголовок
  стал **«Аудит-лог»** (`audit/list.html`). Обновить ассерт под новый заголовок.
- `tests/unit/admin/test_users_handler.py:189` — ждёт `"Статистика"`, в `users/detail.html`
  этого слова больше нет. Обновить ассерт под фактический заголовок блока статистики в v2.
- `tests/unit/admin/test_users_handler.py:148` — ждёт точную разметку `"<td>7</td>"`, а ячейка
  теперь `<td class="pv-c-num">{{ pred_count }}</td>`. Сделать ассерт устойчивым к классам
  (например, проверять число/наличие `pv-c-num`, а не точную строку `<td>7</td>`).

### 2. Прогнать ВЕСЬ admin-набор и починить остальное

Я не могу прогнать pytest локально (песочница cowork не качает Python-тулчейн), поэтому список
выше — только то, что нашлось статически. **Обязательно** прогнать полностью и починить всё
красное, обратив внимание на data-зависимые ассерты, которые могла затронуть реверстка:

```bash
uv run pytest tests/unit/admin/ -q
```

Особое внимание: `test_users_handler.py` (`@alice` на 147, `✅ сбылся` на 187, block/unblock
action на 245/246/265/266) и `test_audit_handler.py` (`event.create` на 147, `Свернуть` на 186/207,
`hx-get=".../details"` на 208). Где сломалось из-за нового текста/разметки — обновить тест под v2;
**логику не менять**.

### 3. Поправить archive-конвенцию

В ветке задача заархивирована плоским файлом `handoff/archive/TASK-056.md`. Должно быть
`handoff/archive/TASK-056-admin-v2-users-audit/task.md` (директория с `task.md`), как у TASK-053/054/055.

## Definition of Done (поправки)

- [ ] `uv run pytest` — **зелёный полностью** (не только admin-набор).
- [ ] `ruff check` / `ruff format --check` / `mypy` — чисто.
- [ ] PR #114 обновлён, **CI зелёный**, ветка **смёрджена в `main`** (squash).
- [ ] Archive: `handoff/archive/TASK-056-admin-v2-users-audit/task.md` (директория), плоского
      `archive/TASK-056.md` не остаётся; отчёт в `handoff/outbox/TASK-056-report.md` (уже есть).
- [ ] Локальная `main` синхронизирована с `origin/main`.

## Замечание на будущее

Отчёт снова «Открытые вопросы: Нет» при незелёном CI и оставленной несмёрдженной ветке — это
противоречит DoD. Если CI красный или PR не смёрджен — задача **не закрыта**; это нужно явно
указывать в отчёте, а не отмечать «всё готово».
