# Решения — task-018-review

**4 keep + 1 в тех-долг:**

| # | Решение | Альтернативы | Обоснование |
|---|---|---|---|
| 1 | `fresh_db`-фикстура остаётся «осторожной» с TRUNCATE в самом тесте, **но добавлен пункт в BACKLOG**: «`fresh_db` должна делать `TRUNCATE ... CASCADE` перед `downgrade base`» | Сейчас же ужесточить fresh_db в отдельной задаче | Текущий обход через TRUNCATE в тесте `test_0003_relax_event_archive_check` достаточен для CI. Жёсткая изоляция нужна, если в будущем появятся ещё несколько тестов с похожим паттерном (миграция-зависимые INSERT'ы). Триггер для фикса — второй похожий случай |
| 2 | Migration 0003 downgrade остаётся **fail-loud** (не auto-fix данных) | `DELETE FROM event WHERE is_archived=true AND result_outcome_id IS NULL` в downgrade с комментарием «destructive» | Destructive auto-fix опасен на проде. Если оператор делает `downgrade -1` после периода работы продукта — пусть осознанно решит, что делать с архивными без итога (вручную проставить result_outcome_id, разархивировать, или удалить). Текущая ошибка валидации в downgrade — правильный fail-loud |
| 3 | `tests/integration/test_migrations.py` использует `text(sql)` для INSERT — оставить | Унифицировать через ORM-фабрики | Прямой SQL более явный для миграционного теста, не зависит от состояния ORM-моделей (которые могут быть впереди миграционной цепочки в локальной разработке). Стиль теста соответствует «alembic через subprocess uv run alembic» из TASK-006 review |
| 4 | `is_blocked` фильтр пользователя в `find_candidates` — закрытый кейс из TASK-017 review | — | Подтверждено в `state/DECISIONS.md` (строка 2026-05-24). Закрываем |

## Итоги Этапа 2

Зафиксированы в `sessions/2026-05-24-05-task-018-review/brief.md` (раздел «Итоги Этапа 2»). Закрывает все 9 задач серии TASK-010..018.
